# Build grade span string from grade flags

Constructs a grade span representation (e.g., "K-5", "6-8", "9-12") from
binary grade offering flags.

## Usage

``` r
build_grade_span(...)
```

## Arguments

- ...:

  Grade flag columns (prekindergarten through grade_12)

## Value

Character vector of grade spans
