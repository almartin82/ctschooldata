# Tidy wide-format enrollment data

Transforms wide enrollment data to long format with subgroup column.

## Usage

``` r
tidy_wide_data(df)
```

## Arguments

- df:

  A wide data.frame with demographic columns and grade_XX columns

## Value

A long data.frame of tidied enrollment data
