# Tidy pre-tidied enrollment data

Handles data that is already in a semi-tidy format (e.g., CTData.org
data).

## Usage

``` r
tidy_pretidied_data(df)
```

## Arguments

- df:

  Data frame with n_students and grade_level columns already present

## Value

Standardized tidy data frame
